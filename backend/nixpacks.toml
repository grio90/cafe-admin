[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["chmod +x node_modules/.bin/* || true", "npm run build"]

[start]
cmd = "npx prisma migrate deploy && npm start"
